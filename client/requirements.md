## Packages
jspdf | Client-side PDF generation for legal documents
framer-motion | Smooth page transitions and UI animations
html2canvas | Helper for PDF generation if needed
clsx | Utility for conditional class names (standard with tailwind)
tailwind-merge | Utility for merging tailwind classes (standard with shadcn)

## Notes
Integration with Google Fonts for Urdu support (Noto Nastaliq Urdu)
Auth endpoints: /api/login, /api/register, /api/user
Document generation expects POST to /api/generate
